const newsRouter = require('express').Router()

// import mongoose model
const Othernews = require('../models/othernews')


newsRouter.get('/othernews', (res, req) => {

})

newsRouter.post('/othernews', (res, req) => {

})

newsRouter.get('/othernews/:id', (res, req) => {

})

newsRouter.post('/othernews/:id', (res, req) => {

})

newsRouter.delete('/othernews/:id', (res, req) => {

})
export default newsRouter